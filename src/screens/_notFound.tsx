import { RouteComponentProps } from "@reach/router";
import React from "react";

const NotFound = (_: RouteComponentProps) => <div data-testid="renderedText">Sorry, nothing found.</div>;

export default NotFound;
